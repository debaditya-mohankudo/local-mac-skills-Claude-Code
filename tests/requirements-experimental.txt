# Experimental: LLM agent response quality testing
# Dependencies are managed via pyproject.toml — use uv to install:
#
#   uv sync --extra test
#   uv run pytest tests/test_agent_responses.py -v
#
# Also requires: Claude Code CLI installed and authenticated (no API key needed)
